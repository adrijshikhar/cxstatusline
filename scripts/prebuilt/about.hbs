Generated by cargo-about from the patched openai/codex checkout used to build `codex` and
`codex-code-mode-host`. Each entry lists the crates covered by one license text.

{{#each licenses}}
## {{name}} ({{id}})

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}
{{/each}}

```text
{{text}}
```

{{/each}}
