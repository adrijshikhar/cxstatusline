diff --git a/dist/core.cjs b/dist/core.cjs
index d3f5fff674e5d0364ea1724b41ee9d6122cae90c..633b4ea14c4960345833f94d4011ee0eedef3335 100644
--- a/dist/core.cjs
+++ b/dist/core.cjs
@@ -181,9 +181,10 @@ if (typeof globalThis.setImmediate === "undefined") {
 // src/xterm-ink.tsx
 var CLEAR_TERMINAL = "\x1B[2J\x1B[3J\x1B[H";
 var CURSOR_HOME = "\x1B[H";
+var BSU = "\x1B[?2026h";
+var ESU = "\x1B[?2026l";
 function filterStdoutChunk(str) {
-  str = str.replaceAll(CLEAR_TERMINAL, CURSOR_HOME);
-  return str;
+  return str.includes(CLEAR_TERMINAL) ? `${BSU}${str}${ESU}` : str;
 }
 var getYogaInit = () => {
   if (typeof globalThis !== "undefined" && globalThis.__yogaPromise) {
diff --git a/dist/core.js b/dist/core.js
index d5e41f1231b083c7e0d1b8f3c1f23881401dfb18..ae02e2fdd56c38dddadde5c969a2238b2044897d 100644
--- a/dist/core.js
+++ b/dist/core.js
@@ -141,9 +141,10 @@ if (typeof globalThis.setImmediate === "undefined") {
 // src/xterm-ink.tsx
 var CLEAR_TERMINAL = "\x1B[2J\x1B[3J\x1B[H";
 var CURSOR_HOME = "\x1B[H";
+var BSU = "\x1B[?2026h";
+var ESU = "\x1B[?2026l";
 function filterStdoutChunk(str) {
-  str = str.replaceAll(CLEAR_TERMINAL, CURSOR_HOME);
-  return str;
+  return str.includes(CLEAR_TERMINAL) ? `${BSU}${str}${ESU}` : str;
 }
 var getYogaInit = () => {
   if (typeof globalThis !== "undefined" && globalThis.__yogaPromise) {
diff --git a/dist/index.js b/dist/index.js
index eb406bb17f4d925b73a1c31cde0c5c7c9f8b4c50..b3d702f89f4b01493e2af3427c79dc22b51544bc 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -9037,9 +9037,10 @@ if (typeof globalThis.setImmediate === "undefined") {
 // src/xterm-ink.tsx
 var CLEAR_TERMINAL = "\x1B[2J\x1B[3J\x1B[H";
 var CURSOR_HOME = "\x1B[H";
+var BSU = "\x1B[?2026h";
+var ESU = "\x1B[?2026l";
 function filterStdoutChunk(str) {
-  str = str.replaceAll(CLEAR_TERMINAL, CURSOR_HOME);
-  return str;
+  return str.includes(CLEAR_TERMINAL) ? `${BSU}${str}${ESU}` : str;
 }
 var getYogaInit = () => {
   if (typeof globalThis !== "undefined" && globalThis.__yogaPromise) {
diff --git a/src/xterm-ink.tsx b/src/xterm-ink.tsx
index 41564c87c77b51b7408e2190f53b5e2b2bb5becc..e66b6b8791fcb071fc62435f5e6d30dbd1b1e255 100644
--- a/src/xterm-ink.tsx
+++ b/src/xterm-ink.tsx
@@ -12,10 +12,9 @@ export const CURSOR_HOME = '\x1b[H'
 export const BSU = '\x1b[?2026h' // Begin Synchronized Update (DEC private mode 2026)
 export const ESU = '\x1b[?2026l' // End Synchronized Update
 
-/** Strip/replace ANSI sequences that cause visual glitches in xterm.js. */
+/** Keep full redraws atomic so stale cells never remain visible. */
 export function filterStdoutChunk(str: string): string {
-  str = str.replaceAll(CLEAR_TERMINAL, CURSOR_HOME)
-  return str
+  return str.includes(CLEAR_TERMINAL) ? `${BSU}${str}${ESU}` : str
 }
 
 // Helper to check if yoga init is available
@@ -77,9 +76,8 @@ export function mountInkInXterm(element: React.ReactElement, opts: InkWebOptions
   // In xterm.js this causes a full-screen flash because the clear and redraw
   // are rendered in separate animation frames.
   //
-  // Fix: strip clearTerminal sequences and replace with a cursor-home so the
-  // new content overwrites in place without a visible blank frame.
-  // See filterStdoutChunk() at module scope.
+  // Keep the clear and redraw in one synchronized xterm update. Removing the
+  // clear leaves stale cells when Ink renders a differently sized screen.
   const stdoutBase = new Writable()
   stdoutBase.write = (chunk: unknown, encoding?: any, cb?: any) => {
     let str = typeof chunk === 'string' ? chunk : String(chunk)
